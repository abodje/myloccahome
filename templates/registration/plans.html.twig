<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Choisissez votre plan - MYLOCCA</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 50px 0;
        }
        .pricing-card {
            border-radius: 15px;
            transition: transform 0.3s ease;
            height: 100%;
        }
        .pricing-card:hover {
            transform: translateY(-10px);
        }
        .pricing-card.popular {
            border: 3px solid #ffc107;
            box-shadow: 0 10px 40px rgba(255,193,7,0.3);
        }
        .popular-badge {
            position: absolute;
            top: -15px;
            right: 20px;
            background: linear-gradient(135deg, #ffc107, #ff9800);
            color: white;
            padding: 5px 20px;
            border-radius: 20px;
            font-weight: bold;
            font-size: 12px;
        }
        .price {
            font-size: 3rem;
            font-weight: bold;
            color: #5a8db3;
        }
        .price-period {
            font-size: 1rem;
            color: #6c757d;
        }
        .feature-list {
            list-style: none;
            padding: 0;
        }
        .feature-list li {
            padding: 10px 0;
            border-bottom: 1px solid #f0f0f0;
        }
        .feature-list li i {
            color: #28a745;
            margin-right: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- En-tête -->
        <div class="text-center text-white mb-5">
            <h1 class="display-4 fw-bold mb-3">
                <i class="bi bi-building"></i> MYLOCCA
            </h1>
            <p class="lead">Choisissez le plan qui correspond à vos besoins</p>
            <p class="mb-4">🎉 30 jours d'essai gratuit sur tous les plans - Aucune carte bancaire requise</p>
        </div>

        <!-- Plans -->
        <div class="row g-4 mb-5 justify-content-center">
            {% for plan in plans %}
            <div class="col-md-3">
                <div class="card pricing-card {{ plan.isPopular ? 'popular' : '' }}">
                    {% if plan.isPopular %}
                    <div class="popular-badge">
                        ⭐ PLUS POPULAIRE
                    </div>
                    {% endif %}

                    <div class="card-body text-center p-5">
                        <h3 class="card-title mb-4">{{ plan.name }}</h3>
                        <p class="text-muted mb-4">{{ plan.description }}</p>

                        <!-- Prix -->
                        <div class="mb-4">
                            {% if plan.slug == 'freemium' %}
                                <div class="price text-success">GRATUIT</div>
                                <div class="price-period">Pour toujours</div>
                                <div class="mt-2 text-success">
                                    <small>
                                        <i class="bi bi-infinity"></i>
                                        Aucun engagement, aucun paiement
                                    </small>
                                </div>
                            {% else %}
                                <div class="price">{{ (plan.monthlyPrice / 1000)|number_format(0) }}K</div>
                                <div class="price-period">FCFA / mois</div>
                                {% if plan.yearlyPrice %}
                                <div class="mt-2 text-success">
                                    <small>
                                        <i class="bi bi-tag-fill"></i>
                                        Économisez {{ plan.yearlyDiscount|number_format(0) }}% avec l'annuel
                                    </small>
                                </div>
                                {% endif %}
                            {% endif %}
                        </div>

                        <!-- Limites -->
                        <div class="mb-4 text-start">
                            <ul class="list-unstyled">
                                <li class="mb-2">
                                    <i class="bi bi-building text-primary"></i>
                                    <strong>{{ plan.maxProperties ?? '∞' }}</strong> propriétés
                                </li>
                                <li class="mb-2">
                                    <i class="bi bi-people text-primary"></i>
                                    <strong>{{ plan.maxTenants ?? '∞' }}</strong> locataires
                                </li>
                                <li class="mb-2">
                                    <i class="bi bi-person-badge text-primary"></i>
                                    <strong>{{ plan.maxUsers ?? '∞' }}</strong> utilisateurs
                                </li>
                                <li class="mb-2">
                                    <i class="bi bi-file-earmark text-primary"></i>
                                    <strong>{{ plan.maxDocuments ?? '∞' }}</strong> documents
                                </li>
                            </ul>
                        </div>

                        <!-- Fonctionnalités -->
                        <div class="mb-4 text-start">
                            <h6 class="fw-bold mb-3">Fonctionnalités incluses :</h6>
                            <ul class="feature-list">
                                {% for feature in plan.features %}
                                <li>
                                    <i class="bi {{ feature_icon(feature) }} text-success"></i>
                                    {{ feature_label(feature) }}
                                </li>
                                {% endfor %}
                            </ul>
                        </div>

                        <!-- Bouton -->
                        {% if plan.slug == 'freemium' %}
                            <a href="{{ path('app_registration_register', {'planSlug': plan.slug}) }}"
                               class="btn btn-success btn-lg w-100">
                                <i class="bi bi-gift me-2"></i>
                                Commencer GRATUITEMENT
                            </a>

                            <div class="mt-3">
                                <small class="text-success">
                                    <i class="bi bi-infinity"></i>
                                    Gratuit pour toujours
                                </small>
                            </div>
                        {% else %}
                            <a href="{{ path('app_registration_register', {'planSlug': plan.slug}) }}"
                               class="btn btn-{{ plan.isPopular ? 'warning' : 'primary' }} btn-lg w-100">
                                <i class="bi bi-rocket-takeoff me-2"></i>
                                Commencer l'essai gratuit
                            </a>

                            <div class="mt-3">
                                <small class="text-muted">
                                    <i class="bi bi-shield-check"></i>
                                    Période d'essai de {{ plan.trialDays }} jours
                                </small>
                            </div>
                        {% endif %}
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>

        <!-- Comparaison -->
        <div class="card bg-white shadow-lg">
            <div class="card-body p-5">
                <h3 class="text-center mb-4">
                    <i class="bi bi-question-circle"></i>
                    Questions fréquentes
                </h3>
                <div class="row">
                    <div class="col-md-6">
                        <h5>💳 Quand serai-je facturé ?</h5>
                        <p class="text-muted">
                            Jamais pendant la période d'essai de 30 jours ! Vous ne serez facturé qu'après
                            validation de votre abonnement.
                        </p>
                    </div>
                    <div class="col-md-6">
                        <h5>🔄 Puis-je changer de plan ?</h5>
                        <p class="text-muted">
                            Oui ! Vous pouvez upgrader ou downgrader votre plan à tout moment depuis
                            votre tableau de bord.
                        </p>
                    </div>
                    <div class="col-md-6">
                        <h5>❌ Puis-je annuler ?</h5>
                        <p class="text-muted">
                            Absolument ! Annulez à tout moment sans frais. Votre accès reste actif
                            jusqu'à la fin de la période payée.
                        </p>
                    </div>
                    <div class="col-md-6">
                        <h5>💰 Quels moyens de paiement ?</h5>
                        <p class="text-muted">
                            Nous acceptons Mobile Money, cartes bancaires et virements via CinetPay.
                        </p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Pied de page -->
        <div class="text-center text-white mt-5">
            <p>
                <a href="{{ path('app_login') }}" class="text-white text-decoration-underline">
                    Vous avez déjà un compte ? Connectez-vous
                </a>
            </p>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

