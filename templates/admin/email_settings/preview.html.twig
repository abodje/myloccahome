{% extends 'base.html.twig' %}

{% block title %}Aperçu du template - {{ template_type }}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-eye me-2"></i>
                            Aperçu du template - {{ template_type|capitalize }}
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="alert alert-info">
                            <i class="bi bi-info-circle me-2"></i>
                            Ceci est un aperçu avec des données fictives. Les variables ont été remplacées par des exemples.
                        </div>
                        
                        <div class="border rounded p-3" style="background-color: #f8f9fa;">
                            {{ template_content|raw }}
                        </div>
                        
                        <div class="mt-3">
                            <a href="{{ path('admin_email_settings_index') }}" class="btn btn-secondary">
                                <i class="bi bi-arrow-left me-2"></i>
                                Retour aux paramètres
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
