{% extends 'base.html.twig' %}

{% block title %}Aperçu du contrat - LOKAPRO{% endblock %}

{% block page_title %}
    <h5 class="mb-0">
        <i class="bi bi-eye me-2"></i>
        Aperçu du contrat de bail
    </h5>
{% endblock %}

{% block page_actions %}
    <a href="{{ path('app_admin_contract_config_index') }}" class="btn btn-secondary btn-sm">
        <i class="bi bi-arrow-left"></i> Retour
    </a>
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <h6 class="mb-0">Configuration actuelle</h6>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <i class="bi bi-info-circle me-2"></i>
                        <strong>Note :</strong> Cet aperçu montre la configuration actuelle. Pour voir un contrat complet, générez un contrat de bail depuis la section "Baux".
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <h6>📝 Apparence</h6>
                            <ul class="small">
                                <li><strong>Langue :</strong> {{ config.contract_language|upper }}</li>
                                <li><strong>Titre :</strong> {{ config.contract_main_title }}</li>
                                <li><strong>Police :</strong> {{ config.contract_font_family }}</li>
                                <li><strong>Taille :</strong> {{ config.contract_font_size }}</li>
                            </ul>
                        </div>
                        <div class="col-md-6">
                            <h6>🎨 Couleurs</h6>
                            <div class="d-flex gap-3">
                                <div class="text-center">
                                    <div class="color-box" style="width: 40px; height: 40px; background-color: {{ config.contract_primary_color }}; border-radius: 5px; margin: 0 auto 5px;"></div>
                                    <small>Principal</small>
                                </div>
                                <div class="text-center">
                                    <div class="color-box" style="width: 40px; height: 40px; background-color: {{ config.contract_info_bg_color }}; border-radius: 5px; margin: 0 auto 5px; border: 1px solid #ddd;"></div>
                                    <small>Fond info</small>
                                </div>
                                <div class="text-center">
                                    <div class="color-box" style="width: 40px; height: 40px; background-color: {{ config.contract_highlight_color }}; border-radius: 5px; margin: 0 auto 5px; border: 1px solid #ddd;"></div>
                                    <small>Surbrillance</small>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="row mt-3">
                        <div class="col-md-6">
                            <h6>🏢 Entreprise</h6>
                            <ul class="small">
                                <li><strong>Nom :</strong> {{ config.contract_company_name }}</li>
                                <li><strong>Adresse :</strong> {{ config.contract_company_address|nl2br }}</li>
                                {% if config.contract_logo_url %}
                                    <li><strong>Logo :</strong> <a href="{{ config.contract_logo_url }}" target="_blank">Voir l'image</a></li>
                                {% endif %}
                            </ul>
                        </div>
                        <div class="col-md-6">
                            <h6>✍️ Signatures</h6>
                            <ul class="small">
                                <li><strong>Bailleur :</strong> {{ config.contract_signature_landlord_title }}</li>
                                <li><strong>Locataire :</strong> {{ config.contract_signature_tenant_title }}</li>
                                <li><strong>Lieu :</strong> {{ config.contract_signature_place }}</li>
                            </ul>
                        </div>
                    </div>

                    <div class="mt-4">
                        <h6>📋 Sections du contrat</h6>
                        <div class="row">
                            <div class="col-md-6">
                                <ol class="small">
                                    <li>{{ config.contract_section_1_title }}</li>
                                    <li>{{ config.contract_section_2_title }}</li>
                                    <li>{{ config.contract_section_3_title }}</li>
                                    <li>{{ config.contract_section_4_title }}</li>
                                </ol>
                            </div>
                            <div class="col-md-6">
                                <ol class="small" start="5">
                                    <li>{{ config.contract_section_5_title }}</li>
                                    <li>{{ config.contract_section_6_title }}</li>
                                    <li>{{ config.contract_section_7_title }}</li>
                                    <li>{{ config.contract_section_8_title }}</li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="card mt-3">
                <div class="card-header">
                    <h6 class="mb-0">🎯 Test de génération</h6>
                </div>
                <div class="card-body">
                    <p class="mb-3">Pour tester la configuration, vous pouvez :</p>
                    <ul>
                        <li>Aller dans <strong>Baux</strong> → Créer un nouveau bail</li>
                        <li>Payer une caution pour générer automatiquement le contrat</li>
                        <li>Ou utiliser la commande de test : <code>php bin/console app:test-contract</code></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
