{% extends 'base.html.twig' %}

{% block title %}Gestion des utilisateurs - Administration{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2>👥 Gestion des utilisateurs</h2>
                <a href="{{ path('app_admin_user_new') }}" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Nouvel utilisateur
                </a>
            </div>

            <!-- Statistiques -->
            <div class="row mb-4">
                <div class="col-md-3">
                    <div class="card text-center">
                        <div class="card-body">
                            <h3 class="text-primary">{{ stats.total }}</h3>
                            <small class="text-muted">Total</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-2">
                    <div class="card text-center">
                        <div class="card-body">
                            <h3 class="text-success">{{ stats.active }}</h3>
                            <small class="text-muted">Actifs</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-2">
                    <div class="card text-center">
                        <div class="card-body">
                            <h3 class="text-danger">{{ stats.admins }}</h3>
                            <small class="text-muted">Admins</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-2">
                    <div class="card text-center">
                        <div class="card-body">
                            <h3 class="text-warning">{{ stats.managers }}</h3>
                            <small class="text-muted">Gestionnaires</small>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card text-center">
                        <div class="card-body">
                            <h3 class="text-info">{{ stats.tenants }}</h3>
                            <small class="text-muted">Locataires</small>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Filtres -->
            <div class="card mb-4">
                <div class="card-body">
                    <div class="btn-group" role="group">
                        <a href="{{ path('app_admin_user_index') }}"
                           class="btn btn-{{ current_role is null ? 'primary' : 'outline-primary' }}">
                            Tous
                        </a>
                        <a href="{{ path('app_admin_user_index', {role: 'ROLE_ADMIN'}) }}"
                           class="btn btn-{{ current_role == 'ROLE_ADMIN' ? 'danger' : 'outline-danger' }}">
                            Administrateurs
                        </a>
                        <a href="{{ path('app_admin_user_index', {role: 'ROLE_MANAGER'}) }}"
                           class="btn btn-{{ current_role == 'ROLE_MANAGER' ? 'warning' : 'outline-warning' }}">
                            Gestionnaires
                        </a>
                        <a href="{{ path('app_admin_user_index', {role: 'ROLE_TENANT'}) }}"
                           class="btn btn-{{ current_role == 'ROLE_TENANT' ? 'info' : 'outline-info' }}">
                            Locataires
                        </a>
                    </div>
                </div>
            </div>

            <!-- Liste des utilisateurs -->
            <div class="card">
                <div class="card-header" style="background-color: var(--primary-color); color: white;">
                    <h5 class="mb-0">Liste des utilisateurs</h5>
                </div>
                <div class="card-body p-0">
                    {% if users|length > 0 %}
                        <div class="table-responsive">
                            <table class="table table-hover mb-0">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Nom complet</th>
                                        <th>Email</th>
                                        <th>Téléphone</th>
                                        <th>Rôle(s)</th>
                                        <th>Statut</th>
                                        <th>Dernière connexion</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {% for user in users %}
                                    <tr>
                                        <td>{{ user.id }}</td>
                                        <td>
                                            <strong>{{ user.fullName }}</strong>
                                            {% if user.tenant %}
                                                <br><small class="text-muted">
                                                    <i class="bi bi-person"></i> Lié au locataire #{{ user.tenant.id }}
                                                </small>
                                            {% endif %}
                                            {% if user.owner %}
                                                <br><small class="text-muted">
                                                    <i class="bi bi-building"></i> Lié au gestionnaire #{{ user.owner.id }}
                                                </small>
                                            {% endif %}
                                        </td>
                                        <td>{{ user.email }}</td>
                                        <td>{{ user.phone ?? '-' }}</td>
                                        <td>
                                            {% for role in user.roles %}
                                                {% if role == 'ROLE_ADMIN' %}
                                                    <span class="badge bg-danger">Admin</span>
                                                {% elseif role == 'ROLE_MANAGER' %}
                                                    <span class="badge bg-warning">Gestionnaire</span>
                                                {% elseif role == 'ROLE_TENANT' %}
                                                    <span class="badge bg-info">Locataire</span>
                                                {% elseif role != 'ROLE_USER' %}
                                                    <span class="badge bg-secondary">{{ role }}</span>
                                                {% endif %}
                                            {% endfor %}
                                        </td>
                                        <td>
                                            <span class="badge bg-{{ user.isActive ? 'success' : 'secondary' }}">
                                                {{ user.isActive ? 'Actif' : 'Inactif' }}
                                            </span>
                                        </td>
                                        <td>
                                            {% if user.lastLoginAt %}
                                                {{ user.lastLoginAt|date('d/m/Y H:i') }}
                                            {% else %}
                                                <span class="text-muted">Jamais connecté</span>
                                            {% endif %}
                                        </td>
                                        <td>
                                            <div class="btn-group btn-group-sm">
                                                <a href="{{ path('app_admin_user_show', {id: user.id}) }}"
                                                   class="btn btn-outline-primary" title="Voir">
                                                    <i class="bi bi-eye"></i>
                                                </a>
                                                <a href="{{ path('app_admin_user_edit', {id: user.id}) }}"
                                                   class="btn btn-outline-warning" title="Modifier">
                                                    <i class="bi bi-pencil"></i>
                                                </a>
                                                <form method="POST" action="{{ path('app_admin_user_toggle', {id: user.id}) }}" class="d-inline">
                                                    <button type="submit"
                                                            class="btn btn-outline-{{ user.isActive ? 'secondary' : 'success' }}"
                                                            title="{{ user.isActive ? 'Désactiver' : 'Activer' }}">
                                                        <i class="bi bi-{{ user.isActive ? 'pause' : 'play' }}-circle"></i>
                                                    </button>
                                                </form>
                                                {% if user != app.user %}
                                                <form method="POST" action="{{ path('app_admin_user_delete', {id: user.id}) }}"
                                                      class="d-inline" onsubmit="return confirm('Confirmer la suppression ?')">
                                                    <button type="submit" class="btn btn-outline-danger" title="Supprimer">
                                                        <i class="bi bi-trash"></i>
                                                    </button>
                                                </form>
                                                {% endif %}
                                            </div>
                                        </td>
                                    </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    {% else %}
                        <div class="text-center p-5">
                            <i class="bi bi-people display-1 text-muted"></i>
                            <h5 class="mt-3 text-muted">Aucun utilisateur trouvé</h5>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
