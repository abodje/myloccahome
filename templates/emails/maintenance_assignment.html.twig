<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #0d6efd; color: white; padding: 20px; text-align: center; }
        .content { background: #f8f9fa; padding: 20px; margin: 20px 0; }
        .info-box { background: white; padding: 15px; margin: 10px 0; border-left: 4px solid #0d6efd; }
        .priority-urgent { border-left-color: #dc3545; }
        .priority-high { border-left-color: #fd7e14; }
        .footer { text-align: center; padding: 20px; font-size: 12px; color: #666; }
        .button { display: inline-block; padding: 10px 20px; background: #0d6efd; color: white; text-decoration: none; border-radius: 5px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🔧 Nouvelle demande de maintenance</h1>
        </div>

        <div class="content">
            <p>Bonjour {{ user.firstName }},</p>

            <p>Une demande de maintenance vous a été attribuée :</p>

            <div class="info-box {% if request.priority == 'Urgent' %}priority-urgent{% elseif request.priority == 'Haute' %}priority-high{% endif %}">
                <h3>{{ request.title }}</h3>
                <p><strong>Priorité :</strong> <span style="color: {% if request.priority == 'Urgent' %}#dc3545{% elseif request.priority == 'Haute' %}#fd7e14{% else %}#198754{% endif %}">{{ request.priority }}</span></p>
                <p><strong>Catégorie :</strong> {{ request.category }}</p>
                <p><strong>Propriété :</strong> {{ request.property.fullAddress }}</p>
                {% if request.tenant %}
                    <p><strong>Locataire :</strong> {{ request.tenant.fullName }}</p>
                {% endif %}
                <p><strong>Description :</strong><br>{{ request.description }}</p>
                {% if request.scheduledDate %}
                    <p><strong>Date prévue :</strong> {{ request.scheduledDate|date('d/m/Y H:i') }}</p>
                {% endif %}
            </div>

            <p style="text-align: center; margin: 20px 0;">
                <a href="{{ absolute_url(path('app_maintenance_request_show', {id: request.id})) }}" class="button">
                    Voir la demande
                </a>
            </p>

            <p>Merci de traiter cette demande dans les meilleurs délais.</p>
        </div>

        <div class="footer">
            <p>{{ company.company_name ?? 'LOKAPRO Gestion' }}<br>
            {{ company.company_address ?? '' }}<br>
            {{ company.company_phone ?? '' }}</p>
        </div>
    </div>
</body>
</html>

