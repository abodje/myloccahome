{% extends 'base.html.twig' %}

{% block title %}Inventaires - {{ property.fullAddress }}{% endblock %}

{% block page_title %}
    <h5 class="mb-0">
        <i class="bi bi-clipboard-check me-2"></i>
        Inventaires - {{ property.fullAddress }}
    </h5>
{% endblock %}

{% block page_actions %}
    <a href="{{ path('app_property_show', {id: property.id}) }}" class="btn btn-outline-secondary btn-sm">
        <i class="bi bi-arrow-left"></i> Retour
    </a>
    <a href="#" class="btn btn-primary btn-sm">
        <i class="bi bi-plus-circle"></i> Nouvel inventaire
    </a>
{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h6 class="mb-0">États des lieux de la propriété</h6>
        </div>
        <div class="card-body">
            {% if inventories is empty %}
                <div class="text-center py-5">
                    <i class="bi bi-clipboard display-1 text-muted"></i>
                    <p class="text-muted mt-3">Aucun inventaire pour cette propriété</p>
                    <a href="#" class="btn btn-primary">
                        <i class="bi bi-plus-circle"></i> Créer le premier inventaire
                    </a>
                </div>
            {% else %}
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>Type</th>
                                <th>Date</th>
                                <th>Locataire</th>
                                <th>Statut</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for inventory in inventories %}
                                <tr>
                                    <td>
                                        <i class="bi bi-clipboard-check"></i>
                                        {{ inventory.type }}
                                    </td>
                                    <td>{{ inventory.inventoryDate|date('d/m/Y') }}</td>
                                    <td>
                                        {% if inventory.lease and inventory.lease.tenant %}
                                            {{ inventory.lease.tenant.fullName }}
                                        {% else %}
                                            <span class="text-muted">-</span>
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% set status = inventory.items|length > 0 ? 'En cours' : 'En attente' %}
                                        {% if status == 'En cours' %}
                                            <span class="badge bg-warning">{{ status }}</span>
                                        {% else %}
                                            <span class="badge bg-secondary">{{ status }}</span>
                                        {% endif %}
                                    </td>
                                    <td>
                                        <div class="btn-group btn-group-sm">
                                            <a href="#" class="btn btn-outline-primary" title="Voir">
                                                <i class="bi bi-eye"></i>
                                            </a>
                                            <a href="#" class="btn btn-outline-secondary" title="Modifier">
                                                <i class="bi bi-pencil"></i>
                                            </a>
                                            <a href="#" class="btn btn-outline-success" title="Télécharger">
                                                <i class="bi bi-download"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            {% endif %}
        </div>
    </div>

    <div class="card mt-3">
        <div class="card-header">
            <h6 class="mb-0">📝 À propos des inventaires</h6>
        </div>
        <div class="card-body">
            <p class="mb-2"><strong>État des lieux d'entrée :</strong> Réalisé lors de la remise des clés au locataire</p>
            <p class="mb-2"><strong>État des lieux de sortie :</strong> Réalisé à la fin du bail</p>
            <p class="mb-0"><strong>Importance :</strong> Document obligatoire servant de preuve en cas de litige sur l'état du logement</p>
        </div>
    </div>
{% endblock %}

