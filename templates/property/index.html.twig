{% extends 'base.html.twig' %}

{% block title %}Mes biens - MYLOCCA{% endblock %}

{% block page_title %}
    <h5 class="mb-0">
        <i class="bi bi-house me-2"></i>
        Mes biens
    </h5>
{% endblock %}

{% block page_actions %}
    <div class="d-flex gap-2">
        <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#newRequestModal">
            Faire une demande
        </button>
        {% if not is_tenant_view %}
        <a href="{{ path('app_property_new') }}" class="btn btn-outline-primary">
            <i class="bi bi-plus-circle me-2"></i>
            Ajouter un bien
        </a>
        {% endif %}
    </div>
{% endblock %}

{% block body %}
    <!-- Filtres -->
    <div class="row mb-4">
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    <form method="GET" class="row g-3">
                        <div class="col-md-3">
                            <label class="form-label">Recherche</label>
                            <input type="text" name="search" class="form-control form-control-sm"
                                   placeholder="Adresse, ville..." value="{{ search }}">
                        </div>
                        <div class="col-md-2">
                            <label class="form-label">Statut</label>
                            <select name="status" class="form-select form-select-sm">
                                <option value="">Tous</option>
                                <option value="Libre" {{ current_status == 'Libre' ? 'selected' : '' }}>Libre</option>
                                <option value="Occupé" {{ current_status == 'Occupé' ? 'selected' : '' }}>Occupé</option>
                                <option value="En travaux" {{ current_status == 'En travaux' ? 'selected' : '' }}>En travaux</option>
                            </select>
                        </div>
                        <div class="col-md-2">
                            <label class="form-label">Type</label>
                            <select name="type" class="form-select form-select-sm">
                                <option value="">Tous</option>
                                <option value="Appartement" {{ current_type == 'Appartement' ? 'selected' : '' }}>Appartement</option>
                                <option value="Maison" {{ current_type == 'Maison' ? 'selected' : '' }}>Maison</option>
                                <option value="Studio" {{ current_type == 'Studio' ? 'selected' : '' }}>Studio</option>
                                <option value="Bureau" {{ current_type == 'Bureau' ? 'selected' : '' }}>Bureau</option>
                            </select>
                        </div>
                        <div class="col-md-5 d-flex align-items-end">
                            <button type="submit" class="btn btn-outline-primary btn-sm me-2">
                                <i class="bi bi-funnel"></i> Filtrer
                            </button>
                            <a href="{{ path('app_property_index') }}" class="btn btn-outline-secondary btn-sm">
                                <i class="bi bi-x-circle"></i> Reset
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Liste des biens -->
    <div class="card">
        <div class="card-header" style="background-color: var(--primary-color); color: white;">
            <div class="row">
                <div class="col-1"></div>
                <div class="col-2"><strong>Réf. Immeuble</strong></div>
                <div class="col-3"><strong>Adresse</strong></div>
                <div class="col-2"><strong>Ville</strong></div>
                <div class="col-2"><strong>Tantièmes</strong></div>
                <div class="col-2"><strong>Nombre de lots</strong></div>
            </div>
        </div>
        <div class="card-body p-0">
            {% if properties|length > 0 %}
                {% for property in properties %}
                    <div class="row align-items-center p-3 border-bottom property-row"
                         data-property-id="{{ property.id }}"
                         style="cursor: pointer;">
                        <div class="col-1">
                            <button class="btn btn-sm btn-outline-primary expand-btn">
                                <i class="bi bi-chevron-right"></i>
                            </button>
                        </div>
                        <div class="col-2">
                            <strong>{{ ('00000000' ~ property.id)|slice(-8) }}</strong>
                        </div>
                        <div class="col-3">
                            <strong>{{ property.address }}</strong>
                            {% if property.postalCode %}
                                <br><small class="text-muted">{{ property.postalCode }}</small>
                            {% endif %}
                        </div>
                        <div class="col-2">
                            <span>{{ property.city|upper }}</span>
                        </div>
                        <div class="col-2">
                            <span>{{ (property.surface * 100)|number_format(0, ',', '') }}</span>
                        </div>
                        <div class="col-2">
                            <span>{{ property.rooms }}</span>
                        </div>
                    </div>

                    <!-- Détail expandable -->
                    <div class="property-details" id="details-{{ property.id }}" style="display: none;">
                        <div class="bg-light p-3">
                            <div class="card">
                                <div class="card-header" style="background-color: var(--primary-color); color: white;">
                                    <div class="row">
                                        <div class="col-2"><strong>Num. lot</strong></div>
                                        <div class="col-2"><strong>Type de bien</strong></div>
                                        <div class="col-2"><strong>Date de transfert</strong></div>
                                        <div class="col-2"><strong>Pièces</strong></div>
                                        <div class="col-2"><strong>Surface (Carrez)</strong></div>
                                        <div class="col-2"><strong>Tantièmes</strong></div>
                                    </div>
                                </div>
                                <div class="card-body p-3">
                                    <div class="row">
                                        <div class="col-2">{{ ('000' ~ property.id)|slice(-3) }}</div>
                                        <div class="col-2">{{ property.propertyType }}</div>
                                        <div class="col-2">--</div>
                                        <div class="col-2">{{ property.rooms }}</div>
                                        <div class="col-2">{{ property.surface }} m²</div>
                                        <div class="col-2">{{ (property.surface * 100)|number_format(0, ',', '') }} / {{ (property.surface * 100)|number_format(0, ',', '') }}</div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            {% else %}
                <div class="text-center p-5">
                    <i class="bi bi-house display-1 text-muted"></i>
                    <h5 class="mt-3 text-muted">Aucun bien trouvé</h5>
                    <p class="text-muted">Commencez par ajouter votre premier bien immobilier.</p>
                    <a href="{{ path('app_property_new') }}" class="btn btn-primary">
                        <i class="bi bi-plus-circle me-2"></i>
                        Ajouter un bien
                    </a>
                </div>
            {% endif %}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <!-- Modal Information immeuble -->
    <div class="modal fade" id="propertyInfoModal" tabindex="-1">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Information immeuble</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div id="modal-property-content">
                        <!-- Contenu chargé dynamiquement -->
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal nouvelle demande -->
    <div class="modal fade" id="newRequestModal" tabindex="-1">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Nouvelle demande</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="row g-3">
                        <div class="col-md-6">
                            <div class="card h-100 request-type-card" data-type="payment">
                                <div class="card-body text-center">
                                    <i class="bi bi-credit-card display-4 text-primary mb-3"></i>
                                    <h6>Paiements, charges, dépôt de garantie</h6>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="card h-100 request-type-card" data-type="maintenance">
                                <div class="card-body text-center">
                                    <i class="bi bi-tools display-4 text-warning mb-3"></i>
                                    <h6>Dépannage, travaux, sinistres</h6>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="card h-100 request-type-card" data-type="documents">
                                <div class="card-body text-center">
                                    <i class="bi bi-file-earmark-text display-4 text-info mb-3"></i>
                                    <h6>Attestations, coordonnées, documents</h6>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="card h-100 request-type-card" data-type="access">
                                <div class="card-body text-center">
                                    <i class="bi bi-key display-4 text-success mb-3"></i>
                                    <h6>Accès et entretien de l'immeuble</h6>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <style>
        .property-row:hover {
            background-color: #f8f9fa;
        }

        .expand-btn {
            transition: transform 0.3s ease;
            border: none;
            background: none;
        }

        .expand-btn:hover {
            background-color: rgba(0, 123, 255, 0.1);
            border-radius: 50%;
        }

        .expand-btn.expanded {
            transform: rotate(90deg);
        }

        .property-details {
            transition: all 0.3s ease;
            overflow: hidden;
        }

        .request-type-card {
            cursor: pointer;
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }

        .request-type-card:hover {
            border-color: var(--bs-primary);
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
    </style>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            console.log('Page chargée, recherche des boutons d\'expansion...'); // Debug

            const expandButtons = document.querySelectorAll('.expand-btn');
            console.log('Boutons d\'expansion trouvés:', expandButtons.length); // Debug

            // Gestion de l'expansion des détails - clic direct sur le bouton
            expandButtons.forEach(btn => {
                btn.addEventListener('click', function(e) {
                    e.preventDefault();
                    e.stopPropagation();

                    console.log('Bouton d\'expansion cliqué'); // Debug

                    const propertyId = this.closest('.property-row').dataset.propertyId;
                    const details = document.getElementById('details-' + propertyId);
                    const icon = this.querySelector('i');

                    console.log('Property ID:', propertyId); // Debug
                    console.log('Details element:', details); // Debug

                    if (!details) {
                        console.error('Élément details non trouvé pour property ID:', propertyId);
                        return;
                    }

                    const isHidden = details.style.display === 'none' || details.style.display === '';

                    if (isHidden) {
                        details.style.display = 'block';
                        icon.classList.remove('bi-chevron-right');
                        icon.classList.add('bi-chevron-down');
                        console.log('Détails affichés'); // Debug
                    } else {
                        details.style.display = 'none';
                        icon.classList.remove('bi-chevron-down');
                        icon.classList.add('bi-chevron-right');
                        console.log('Détails masqués'); // Debug
                    }
                });
            });

            // Gestion du clic sur la ligne (pour ouvrir la modal)
            document.querySelectorAll('.property-row').forEach(row => {
                row.addEventListener('click', function(e) {
                    // Ne pas ouvrir la modal si on clique sur le bouton d'expansion
                    if (e.target.closest('.expand-btn')) {
                        return;
                    }

                    // Ouvrir la modal avec les détails
                    openPropertyModal(this.dataset.propertyId);
                });
            });

            // Gestion des types de demandes
            document.querySelectorAll('.request-type-card').forEach(card => {
                card.addEventListener('click', function() {
                    const type = this.dataset.type;
                    // Rediriger vers la création de demande selon le type
                    window.location.href = '{{ path("app_maintenance_request_new") }}';
                });
            });
        });

        function openPropertyModal(propertyId) {
            // Afficher un loader
            document.getElementById('modal-property-content').innerHTML = `
                <div class="text-center p-4">
                    <div class="spinner-border text-primary" role="status">
                        <span class="visually-hidden">Chargement...</span>
                    </div>
                    <p class="mt-2">Chargement des informations...</p>
                </div>
            `;

            // Ouvrir la modal
            const modal = new bootstrap.Modal(document.getElementById('propertyInfoModal'));
            modal.show();

            // Charger les données via AJAX
            fetch(`/mes-biens/api/${propertyId}/details`)
                .then(response => response.json())
                .then(data => {
                    const modalContent = `
                        <div class="row mb-3">
                            <div class="col-12">
                                <label class="form-label text-muted">Adresse du bien</label>
                                <div class="form-control-plaintext border p-2 bg-light">
                                    ${data.address.toUpperCase()}
                                </div>
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-6">
                                <label class="form-label text-muted">Gardien</label>
                                <div class="text-primary">${data.gardien}</div>
                            </div>
                            <div class="col-6">
                                <label class="form-label text-muted">Ascenseur</label>
                                <div class="text-primary">${data.ascenseur}</div>
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-12">
                                <label class="form-label text-muted">Jours de présence du gardien</label>
                                <div>${data.jours_gardien}</div>
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-12">
                                <label class="form-label text-muted">Horaires du gardien</label>
                                <div>${data.horaires_gardien}</div>
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-6">
                                <label class="form-label text-muted">Digicode BAT 1</label>
                                <div>${data.digicode_bat1}</div>
                            </div>
                            <div class="col-6">
                                <label class="form-label text-muted">Digicode ENTREE PRINCIPALE</label>
                                <div>${data.digicode_entree}</div>
                            </div>
                        </div>

                        <h6 class="mt-4 mb-3">Superficie du bien</h6>
                        <div class="border p-3 bg-light">
                            <div class="row mb-2">
                                <div class="col-6">
                                    <label class="form-label text-muted">Surface totale</label>
                                    <div>${data.surface_total} m2</div>
                                </div>
                                <div class="col-6">
                                    <label class="form-label text-muted">Surface (Loi Carrez)</label>
                                    <div>${data.surface_carrez} m2</div>
                                </div>
                            </div>
                        </div>

                        <h6 class="mt-4 mb-3">Descriptif du bien</h6>
                        <div class="border p-3 bg-light">
                            <div class="row mb-2">
                                <div class="col-6">
                                    <label class="form-label text-muted">Numéro du lot</label>
                                    <div>${data.lot_number}</div>
                                </div>
                                <div class="col-6">
                                    <label class="form-label text-muted">Pièces habitables</label>
                                    <div>${data.rooms}</div>
                                </div>
                            </div>
                        </div>
                    `;

                    document.getElementById('modal-property-content').innerHTML = modalContent;
                })
                .catch(error => {
                    console.error('Erreur:', error);
                    document.getElementById('modal-property-content').innerHTML = `
                        <div class="alert alert-danger">
                            <i class="bi bi-exclamation-triangle me-2"></i>
                            Erreur lors du chargement des informations.
                        </div>
                    `;
                });
        }
    </script>
{% endblock %}
